# js-bindEvent-function
A compatible bindEvent base on Javascript(support IE8 webkit moz)

Also support mousewheel

```javascript
// use webkit event standard
bindEvent(el, type, function (event) {
  ...
})
```
